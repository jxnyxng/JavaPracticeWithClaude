/*
 * ============================================================
 * 파일명: P01_IfElse.java
 * 주제: if-else 조건문 (Conditional Statements)
 * ============================================================
 *
 * [ 학습 목표 ]
 * 1. if, else if, else 구조를 이해하고 올바르게 작성할 수 있다.
 * 2. 비교 연산자(>, <, >=, <=, ==, !=)와 논리 연산자(&&, ||, !)를 조건식에 활용할 수 있다.
 * 3. 여러 조건을 단계적으로 분기하여 복잡한 판단 로직을 구현할 수 있다.
 *
 * [ 핵심 개념 ]
 * - if 문: 조건이 true일 때만 코드 블록을 실행한다.
 *   예) if (score >= 90) { System.out.println("A"); }
 *
 * - else if 문: 앞의 조건이 false일 때 추가 조건을 검사한다.
 *   예) else if (score >= 80) { System.out.println("B"); }
 *
 * - else 문: 위의 모든 조건이 false일 때 실행된다.
 *   예) else { System.out.println("F"); }
 *
 * - 중요: 조건은 위에서 아래로 순서대로 평가되므로, 범위가 좁은 조건을 먼저 작성해야 한다.
 *
 * [ 사용법 ]
 * 1. 각 문제의 메소드 안에 있는 // TODO: 주석을 찾는다.
 * 2. 해당 위치에 알맞은 코드를 작성한다.
 * 3. main() 메소드를 실행하여 출력 결과를 확인한다.
 * ============================================================
 */
public class P01_IfElse {

    // ============================================================
    // 문제 1: 성적 등급 판정
    // ============================================================
    /*
     * 제목: 점수로 학점 등급 출력하기
     *
     * 설명:
     *   정수형 점수(0 ~ 100)를 입력받아 해당하는 학점 등급을 반환한다.
     *   등급 기준은 다음과 같다.
     *     - 90 이상        : "A"
     *     - 80 이상 90 미만: "B"
     *     - 70 이상 80 미만: "C"
     *     - 60 이상 70 미만: "D"
     *     - 60 미만        : "F"
     *   점수가 0 미만이거나 100 초과이면 "유효하지 않은 점수" 를 반환한다.
     *
     * 예상 출력:
     *   점수: 95 -> 등급: A
     *   점수: 82 -> 등급: B
     *   점수: 71 -> 등급: C
     *   점수: 65 -> 등급: D
     *   점수: 45 -> 등급: F
     *   점수: -5 -> 등급: 유효하지 않은 점수
     *
     * 힌트:
     *   - 먼저 점수 범위 유효성 검사를 if 문으로 처리한다.
     *   - else if를 사용하여 높은 점수 조건부터 차례로 내려온다.
     *   - 90 이상 조건을 가장 먼저 작성하는 것이 실수를 줄이는 방법이다.
     */
    public static String getGrade(int score) {
        // TODO: 점수 유효성 검사 (0 미만 또는 100 초과이면 "유효하지 않은 점수" 반환)
        if (score < 0 || score > 100) {
            return "유효하지 않은 점수";
        }
        // TODO: 90 이상이면 "A" 반환
        else if (score >= 90) {
            return "A";
        }
        // TODO: 80 이상이면 "B" 반환
        else if (score >= 80) {
            return "B";
        }
        // TODO: 70 이상이면 "C" 반환
        else if (score >= 70) {
            return "C";
        }
        // TODO: 60 이상이면 "D" 반환
        else if (score >= 60) {
            return "D";
        }
        // TODO: 그 외 모두 "F" 반환

        return "F"; // 이 줄을 수정하지 말고, 위 TODO를 채워 올바른 값을 반환하게 하세요.
    }

    // ============================================================
    // 문제 2: 나이별 요금 계산
    // ============================================================
    /*
     * 제목: 나이에 따른 요금 계산하기
     *
     * 설명:
     *   나이(정수)를 입력받아 해당 요금 카테고리 문자열을 반환한다.
     *   요금 기준은 다음과 같다.
     *     - 0 이상 12 이하  : "어린이 요금: 500원"
     *     - 13 이상 18 이하 : "청소년 요금: 1000원"
     *     - 19 이상 64 이하 : "성인 요금: 1500원"
     *     - 65 이상         : "경로 요금: 무료"
     *   나이가 0 미만이면 "유효하지 않은 나이" 를 반환한다.
     *
     * 예상 출력:
     *   나이: 7  -> 어린이 요금: 500원
     *   나이: 15 -> 청소년 요금: 1000원
     *   나이: 30 -> 성인 요금: 1500원
     *   나이: 70 -> 경로 요금: 무료
     *   나이: -1 -> 유효하지 않은 나이
     *
     * 힌트:
     *   - 나이가 음수인 경우를 먼저 처리한다.
     *   - 각 연령대 범위를 && (AND) 연산자로 결합할 수 있다.
     *     예) age >= 13 && age <= 18
     */
    public static String getFare(int age) {
        // TODO: 나이가 0 미만이면 "유효하지 않은 나이" 반환
        if (age < 0) {
            return "유효하지 않은 나이";
        }
        // TODO: 나이가 0~12이면 "어린이 요금: 500원" 반환
        else if (age < 13) {
            return "어린이 요금: 500원";
        }
        // TODO: 나이가 13~18이면 "청소년 요금: 1000원" 반환
        else if (age < 19) {
            return "청소년 요금: 1000원";
        }
        // TODO: 나이가 19~64이면 "성인 요금: 1500원" 반환
        else if (age < 65) {
            return "성인 요금: 1500원";
        }
        // TODO: 나이가 65 이상이면 "경로 요금: 무료" 반환
        return "경로 요금: 무료"; // 이 줄을 수정하지 말고, 위 TODO를 채워 올바른 값을 반환하게 하세요.
    }

    // ============================================================
    // 문제 3: 세 수의 최대값, 최소값, 중간값 찾기
    // ============================================================
    /*
     * 제목: 세 정수에서 최대값 / 최소값 / 중간값 출력하기
     *
     * 설명:
     *   세 개의 정수 a, b, c를 입력받아 최대값, 최소값, 중간값을 구하여 출력한다.
     *   Math.max(), Math.min() 같은 내장 메소드 없이 if-else 만으로 구현한다.
     *
     * 예상 출력 (a=3, b=7, c=5 일 때):
     *   최대값: 7
     *   최소값: 3
     *   중간값: 5
     *
     * 예상 출력 (a=10, b=10, c=5 일 때):
     *   최대값: 10
     *   최소값: 5
     *   중간값: 10
     *
     * 힌트:
     *   - 최대값: a, b, c를 두 개씩 비교하며 if-else로 가장 큰 값을 찾는다.
     *   - 최소값: 마찬가지로 가장 작은 값을 찾는다.
     *   - 중간값: a + b + c - 최대값 - 최소값 으로 간단히 구할 수 있다.
     */
    public static void printMinMaxMid(int a, int b, int c) {
        int max = 0;
        int min = 0;
        int mid = 0;

        // TODO: if-else를 사용하여 max(최대값) 계산
        if (a >= b && a >= c) {
            max = a;
        } else if (b >= a && b >= c) {
            max = b;
        } else if (c >= a && c >= b) {
            max = c;
        }
        // TODO: if-else를 사용하여 min(최소값) 계산
        if (a <= b && a <= c) {
            min = a;
        } else if (b <= a && b <= c) {
            min = b;
        } else if (c <= a && c <= b) {
            min = c;
        }
        // TODO: 중간값(mid) 계산 (힌트: 합계 - max - min)
        mid = (a+b+c) - max - min;
        // TODO: 최대값, 최소값, 중간값을 출력
        System.out.println("최댓값: " + max);
        System.out.println("최소값: " + min);
        System.out.println("중간값: " + mid);
    }

    // ============================================================
    // 문제 4: 양수 / 음수 / 0 판별
    // ============================================================
    /*
     * 제목: 숫자의 부호 판별 후 메시지 출력
     *
     * 설명:
     *   정수 하나를 입력받아 양수, 음수, 0인지 판별하고
     *   각각 다른 메시지를 반환한다.
     *     - 양수 (0 초과): "[숫자]은(는) 양수입니다."
     *     - 음수 (0 미만): "[숫자]은(는) 음수입니다."
     *     - 0            : "0입니다."
     *
     * 예상 출력:
     *   입력: 42  -> "42은(는) 양수입니다."
     *   입력: -7  -> "-7은(는) 음수입니다."
     *   입력: 0   -> "0입니다."
     *
     * 힌트:
     *   - 숫자 > 0, 숫자 < 0, 숫자 == 0 세 가지 경우로 분기한다.
     *   - 문자열 연결(+)을 사용하여 숫자를 메시지에 포함시킨다.
     *     예) number + "은(는) 양수입니다."
     */
    public static String checkSign(int number) {
        // TODO: number가 0보다 크면 "[number]은(는) 양수입니다." 반환
        if (number > 0) {
            return number + "은(는) 양수입니다.";
        }
        // TODO: number가 0보다 작으면 "[number]은(는) 음수입니다." 반환
        else if (number < 0) {
            return number + "은(는) 음수입니다.";
        }
        // TODO: number가 0이면 "0입니다." 반환
        return "0입니다."; // 이 줄을 수정하지 말고, 위 TODO를 채워 올바른 값을 반환하게 하세요.
    }

    // ============================================================
    // 헬퍼 메소드 (내용 직접 구현 불필요 - 시그니처만 참고)
    // ============================================================

    /** 두 정수 중 더 큰 값을 반환한다. */
    private static int max(int x, int y) { return 0; /* 구현 생략 */ }

    /** 두 정수 중 더 작은 값을 반환한다. */
    private static int min(int x, int y) { return 0; /* 구현 생략 */ }

    // ============================================================
    // main 메소드 - 테스트 실행
    // ============================================================
    public static void main(String[] args) {

        System.out.println("=== 문제 1: 성적 등급 판정 ===");
        System.out.println("점수: 95 -> 등급: " + getGrade(95));
        System.out.println("점수: 82 -> 등급: " + getGrade(82));
        System.out.println("점수: 71 -> 등급: " + getGrade(71));
        System.out.println("점수: 65 -> 등급: " + getGrade(65));
        System.out.println("점수: 45 -> 등급: " + getGrade(45));
        System.out.println("점수: -5 -> 등급: " + getGrade(-5));

        System.out.println();
        System.out.println("=== 문제 2: 나이별 요금 계산 ===");
        System.out.println("나이:  7 -> " + getFare(7));
        System.out.println("나이: 15 -> " + getFare(15));
        System.out.println("나이: 30 -> " + getFare(30));
        System.out.println("나이: 70 -> " + getFare(70));
        System.out.println("나이: -1 -> " + getFare(-1));

        System.out.println();
        System.out.println("=== 문제 3: 최대값 / 최소값 / 중간값 ===");
        System.out.print("a=3, b=7, c=5  -> ");
        printMinMaxMid(3, 7, 5);
        System.out.print("a=10, b=10, c=5 -> ");
        printMinMaxMid(10, 10, 5);

        System.out.println();
        System.out.println("=== 문제 4: 부호 판별 ===");
        System.out.println("입력:  42 -> " + checkSign(42));
        System.out.println("입력:  -7 -> " + checkSign(-7));
        System.out.println("입력:   0 -> " + checkSign(0));
    }
}
