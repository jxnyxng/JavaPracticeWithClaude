/*
 * ============================================================
 * 파일명: P04_AssignmentOperators.java
 * 주제: 대입/복합 대입 연산자 (=, +=, -=, *=, /=, %=) 및 비트 연산자
 * ============================================================
 *
 * [학습목표]
 * 1. 복합 대입 연산자(+=, -=, *=, /=, %=)가 일반 연산자의 줄임 표현임을 이해한다.
 * 2. 비트 연산자(&, |, ^, ~, <<, >>)의 동작 원리를 2진수 수준에서 이해한다.
 * 3. 비트 연산으로 홀짝 판별, 2의 거듭제곱 확인 등 실용적 활용법을 익힌다.
 *
 * [핵심개념]
 * - 복합 대입 연산자: 연산과 대입을 결합한 축약 표현
 *     a += b   →   a = a + b
 *     a -= b   →   a = a - b
 *     a *= b   →   a = a * b
 *     a /= b   →   a = a / b
 *     a %= b   →   a = a % b
 *
 * - 비트 연산자: 정수를 2진수 비트 단위로 처리
 *     &  (AND): 두 비트가 모두 1일 때만 1
 *     |  (OR) : 두 비트 중 하나라도 1이면 1
 *     ^  (XOR): 두 비트가 다를 때 1 (같으면 0)
 *     ~  (NOT): 모든 비트를 반전 (~n = -(n+1))
 *     << (왼쪽 시프트): 비트를 왼쪽으로 이동 (n << k = n * 2^k)
 *     >> (오른쪽 시프트): 비트를 오른쪽으로 이동 (n >> k = n / 2^k)
 *
 * - 비트 연산 활용 예:
 *     홀수 판별: (n & 1) == 1  → 마지막 비트가 1이면 홀수
 *     2의 거듭제곱 확인: n > 0 && (n & (n-1)) == 0
 *     2배: n << 1
 *     절반: n >> 1
 * ============================================================
 */

import java.util.Scanner;

public class P04_AssignmentOperators {

    public static void main(String[] args) {
        System.out.println("=== 대입/복합 대입 연산자 및 비트 연산자 연습 ===\n");

        problem1();
        problem2();
        problem3();
        problem4();
    }

    // ============================================================
    // 문제 1: 복합 대입 연산자로 누적 합계 계산
    // ============================================================
    /*
     * [문제 설명]
     * 1부터 특정 수 N까지의 합을 복합 대입 연산자(+=)를 사용하여 계산하세요.
     * 또한 -=, *=, /=, %= 의 동작도 각각 확인하세요.
     *
     * 파트 A) 1~N 누적 합계를 += 로 계산
     * 파트 B) 시작값에서 -= 로 차감 누적
     * 파트 C) 복리 누적 *=, 분할 /=, 나머지 %= 각각 1회씩 적용 시범
     *
     * [예상 출력 예시]
     * N 입력: 5
     * [파트 A - += 누적 합계]
     * 초기 sum = 0
     * sum += 1 → sum = 1
     * sum += 2 → sum = 3
     * sum += 3 → sum = 6
     * sum += 4 → sum = 10
     * sum += 5 → sum = 15
     * 1~5의 합계: 15
     *
     * [파트 B - -= 차감 누적]
     * 초기 value = 100
     * value -= 10 → value = 90
     * value -= 10 → value = 80
     * value -= 10 → value = 70
     * 3회 차감 후: 70
     *
     * [파트 C - 각 복합 대입 연산자 1회 시범]
     * n = 10
     * n *= 3 → 30
     * n /= 4 → 7  (정수 나눗셈)
     * n %= 3 → 1
     *
     * [힌트]
     * - for 루프 없이 수동으로 += 를 N번 작성해도 됩니다 (N이 작을 때)
     * - 또는 for (int i = 1; i <= N; i++) sum += i; 로 반복 사용
     * - 반복문을 아직 배우지 않았다면 파트 A는 직접 += 를 5번 작성하세요
     */
    static void problem1() {
        System.out.println("--- 문제1: 복합 대입 연산자로 누적 계산 ---");

        Scanner sc = new Scanner(System.in);

        // TODO: N을 입력받으세요

        // 파트 A: += 누적 합계
        System.out.println("[파트 A - += 누적 합계]");
        int sum = 0;
        System.out.println("초기 sum = " + sum);
        // TODO: 1부터 N까지 += 로 누적하고, 각 단계를 출력하세요
        // 예) sum += 1; System.out.println("sum += 1 → sum = " + sum);

        // TODO: 최종 합계를 출력하세요
        System.out.println();

        // 파트 B: -= 차감 누적
        System.out.println("[파트 B - -= 차감 누적]");
        int value = 100;
        System.out.println("초기 value = " + value);
        // TODO: value에서 10씩 3번 -= 하고 각 단계를 출력하세요
        // TODO: 3회 차감 후 최종 value를 출력하세요
        System.out.println();

        // 파트 C: 각 복합 대입 연산자 1회 시범
        System.out.println("[파트 C - 각 복합 대입 연산자 1회 시범]");
        int n = 10;
        System.out.println("n = " + n);
        // TODO: n *= 3 을 실행하고 결과를 출력하세요
        // TODO: n /= 4 를 실행하고 결과를 출력하세요
        // TODO: n %= 3 를 실행하고 결과를 출력하세요
        System.out.println();
    }

    // ============================================================
    // 문제 2: 복합 대입 연산자를 일반 연산자로 변환
    // ============================================================
    /*
     * [문제 설명]
     * 복합 대입 연산자 표현을 일반 대입 연산자 표현으로 동일하게 변환하고,
     * 두 방식의 결과가 같음을 확인하세요.
     *
     * 변환 목록:
     *   x += 5   →   x = x + 5
     *   x -= 3   →   x = x - 3
     *   x *= 2   →   x = x * 2
     *   x /= 4   →   x = x / 4
     *   x %= 3   →   x = x % 3
     *
     * [예상 출력 예시]
     * === 복합 대입 연산자 vs 일반 연산자 비교 ===
     * 초기값 a = 20, b = 20
     *
     * a += 5  →  a = 25    |   b = b + 5  →  b = 25    | 결과 같음: true
     * a -= 3  →  a = 22    |   b = b - 3  →  b = 22    | 결과 같음: true
     * a *= 2  →  a = 44    |   b = b * 2  →  b = 44    | 결과 같음: true
     * a /= 4  →  a = 11    |   b = b / 4  →  b = 11    | 결과 같음: true
     * a %= 3  →  a = 2     |   b = b % 3  →  b = 2     | 결과 같음: true
     *
     * [힌트]
     * - a와 b를 같은 초기값으로 시작하여 각 연산 후 a == b 인지 비교하세요
     * - System.out.printf를 활용하면 정렬된 출력이 가능합니다
     */
    static void problem2() {
        System.out.println("--- 문제2: 복합 대입 연산자 vs 일반 연산자 비교 ---");

        // TODO: a와 b를 동일한 초기값(예: 20)으로 선언하세요

        // TODO: a에는 복합 대입 연산자를, b에는 일반 대입 연산자를 사용하여 각 연산을 수행하세요
        // TODO: 각 연산 후 a와 b의 값과 두 값이 같은지(a == b) 출력하세요

        // += 연산
        // TODO: a += 5; 와 b = b + 5; 를 수행하고 결과를 비교 출력하세요

        // -= 연산
        // TODO: a -= 3; 와 b = b - 3; 를 수행하고 결과를 비교 출력하세요

        // *= 연산
        // TODO: a *= 2; 와 b = b * 2; 를 수행하고 결과를 비교 출력하세요

        // /= 연산
        // TODO: a /= 4; 와 b = b / 4; 를 수행하고 결과를 비교 출력하세요

        // %= 연산
        // TODO: a %= 3; 와 b = b % 3; 를 수행하고 결과를 비교 출력하세요

        System.out.println();
    }

    // ============================================================
    // 문제 3: 비트 연산자 기초 (&, |, ^, ~, <<, >>)
    // ============================================================
    /*
     * [문제 설명]
     * 두 정수를 입력받아 비트 연산자 6가지의 결과를 출력하세요.
     * 각 연산의 2진수 표현도 함께 출력하여 동작을 시각적으로 확인하세요.
     *
     * [예상 출력 예시]
     * 첫 번째 정수: 12
     * 두 번째 정수: 10
     *
     * 12의 2진수: 00001100
     * 10의 2진수: 00001010
     *
     * 12 &  10 = 8     (2진수: 00001000)  ← AND: 둘 다 1인 비트만 1
     * 12 |  10 = 14    (2진수: 00001110)  ← OR : 하나라도 1이면 1
     * 12 ^  10 = 6     (2진수: 00000110)  ← XOR: 다른 비트만 1
     * ~12      = -13   (2진수: ...11110011) ← NOT: 모든 비트 반전
     * 12 << 2  = 48    (2진수: 00110000)  ← 왼쪽 시프트 2칸 (×4)
     * 12 >> 2  = 3     (2진수: 00000011)  ← 오른쪽 시프트 2칸 (÷4)
     *
     * [힌트]
     * - Integer.toBinaryString(n) : 정수를 2진수 문자열로 변환
     * - String.format("%8s", binaryStr).replace(' ', '0') : 8자리 0패딩
     * - 비트 연산: a & b, a | b, a ^ b, ~a, a << 2, a >> 2
     */
    static void problem3() {
        System.out.println("--- 문제3: 비트 연산자 기초 ---");

        Scanner sc = new Scanner(System.in);

        // TODO: 두 정수를 입력받으세요 (변수명 예: a, b)

        // TODO: 각 수의 2진수 표현을 출력하세요 (Integer.toBinaryString 활용)

        // TODO: & (AND) 연산 결과와 2진수를 출력하세요
        // TODO: | (OR) 연산 결과와 2진수를 출력하세요
        // TODO: ^ (XOR) 연산 결과와 2진수를 출력하세요
        // TODO: ~ (NOT) 연산 결과와 2진수를 출력하세요 (a에 대해서만)
        // TODO: << 2 (왼쪽 시프트) 결과와 2진수를 출력하세요 (a에 대해서만)
        // TODO: >> 2 (오른쪽 시프트) 결과와 2진수를 출력하세요 (a에 대해서만)

        System.out.println();
    }

    // ============================================================
    // 문제 4: 비트 연산으로 홀짝 판별 및 2의 거듭제곱 확인
    // ============================================================
    /*
     * [문제 설명]
     * 비트 연산을 활용하여 두 가지를 확인하세요.
     *
     * 파트 A) 비트 AND 연산으로 홀수/짝수 판별
     *   - (n & 1) == 1 이면 홀수, (n & 1) == 0 이면 짝수
     *   - % 연산자를 사용하지 않고 비트 연산만으로 판별
     *   - 이유: 홀수는 2진수의 마지막 비트가 반드시 1
     *
     * 파트 B) 비트 AND 연산으로 2의 거듭제곱 확인
     *   - n > 0 && (n & (n - 1)) == 0 이면 2의 거듭제곱
     *   - 원리: 2의 거듭제곱은 2진수에서 1인 비트가 하나뿐
     *     예) 8 = 1000, 8-1=7 = 0111, 8 & 7 = 0000
     *
     * 파트 C) 시프트 연산으로 2의 거듭제곱 수열 생성
     *   - 1 << 0 = 1, 1 << 1 = 2, 1 << 2 = 4, ... 1 << 7 = 128
     *
     * [예상 출력 예시]
     * [파트 A - 비트 연산으로 홀짝 판별]
     * 정수 입력: 7
     * 7의 2진수 마지막 비트: 7 & 1 = 1
     * 7은 홀수입니다. (비트 연산으로 판별)
     *
     * [파트 B - 2의 거듭제곱 확인]
     * 정수 입력: 16
     * 16의 2진수: 00010000
     * 15의 2진수: 00001111
     * 16 & 15 = 0
     * 16은 2의 거듭제곱입니다.
     *
     * [파트 C - 시프트로 2의 거듭제곱 생성]
     * 1 << 0 = 1
     * 1 << 1 = 2
     * 1 << 2 = 4
     * 1 << 3 = 8
     * 1 << 4 = 16
     * 1 << 5 = 32
     * 1 << 6 = 64
     * 1 << 7 = 128
     *
     * [힌트]
     * - 홀짝: (n & 1) == 1 이면 홀수
     * - 2의 거듭제곱: n > 0 && (n & (n - 1)) == 0
     * - 시프트: for (int k = 0; k <= 7; k++) System.out.println("1 << " + k + " = " + (1 << k));
     * - isPowerOfTwo 헬퍼 메소드를 활용하세요
     */
    static void problem4() {
        System.out.println("--- 문제4: 비트 연산 활용 ---");

        Scanner sc = new Scanner(System.in);

        // 파트 A: 비트 연산으로 홀짝 판별
        System.out.println("[파트 A - 비트 연산으로 홀짝 판별]");
        // TODO: 정수를 입력받으세요

        // TODO: (n & 1) 값을 출력하세요
        // TODO: 홀수인지 짝수인지 출력하세요 (% 연산자 사용 금지, & 만 사용)
        System.out.println();

        // 파트 B: 2의 거듭제곱 확인
        System.out.println("[파트 B - 2의 거듭제곱 확인]");
        // TODO: 정수를 입력받으세요

        // TODO: 해당 수와 (해당 수 - 1)의 2진수를 출력하세요
        // TODO: (n & (n-1)) 값을 출력하세요
        // TODO: isPowerOfTwo 헬퍼 메소드로 2의 거듭제곱 여부를 출력하세요
        System.out.println();

        // 파트 C: 시프트 연산으로 2의 거듭제곱 수열 생성
        System.out.println("[파트 C - 시프트로 2의 거듭제곱 생성]");
        // TODO: 1 << 0 부터 1 << 7 까지 각 결과를 출력하세요
        System.out.println();
    }

    // ============================================================
    // 헬퍼 메소드 시그니처 (내용을 직접 구현하세요)
    // ============================================================

    /**
     * 정수가 2의 거듭제곱인지 비트 연산으로 확인합니다.
     * @param n 확인할 정수
     * @return 2의 거듭제곱이면 true, 아니면 false
     */
    static boolean isPowerOfTwo(int n) {
        // TODO: 비트 연산 n > 0 && (n & (n - 1)) == 0 을 활용하여 반환하세요
        return false; // TODO: 올바른 값을 반환하도록 수정하세요
    }

    /**
     * 정수를 8자리 2진수 문자열로 변환합니다.
     * @param n 변환할 정수
     * @return 8자리 2진수 문자열 (예: "00001100")
     */
    static String toBinary8(int n) {
        // TODO: Integer.toBinaryString(n) 과 String.format 을 활용하여 8자리로 만들어 반환하세요
        return ""; // TODO: 올바른 문자열을 반환하도록 수정하세요
    }
}