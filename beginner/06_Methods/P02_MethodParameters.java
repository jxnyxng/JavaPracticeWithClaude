/*
 * =====================================================
 * 파일명: P02_MethodParameters.java
 * 주제: 매개변수와 반환값 (Method Parameters & Return Values)
 * =====================================================
 *
 * 【학습 목표】
 * 1. 기본형(primitive type) 매개변수는 값이 복사되어 전달됨을 이해한다.
 * 2. 배열(참조형) 매개변수는 주소가 전달되어 원본이 변경될 수 있음을 이해한다.
 * 3. 메소드에서 배열을 반환하는 방법을 익힌다.
 *
 * 【핵심 개념】
 * - Call by Value (값에 의한 호출):
 *   기본형 변수를 메소드에 전달하면 값의 복사본이 전달됩니다.
 *   메소드 내에서 매개변수를 변경해도 원본 변수는 영향을 받지 않습니다.
 *   대상: int, double, char, boolean 등 기본형 타입
 *
 * - Call by Reference (참조에 의한 호출):
 *   배열이나 객체를 메소드에 전달하면 메모리 주소(참조값)가 전달됩니다.
 *   메소드 내에서 배열 요소를 변경하면 원본 배열도 변경됩니다.
 *   대상: 배열, 클래스 인스턴스 등 참조형 타입
 *
 * - 메소드 체이닝(Method Chaining):
 *   메소드의 반환값을 즉시 다른 메소드의 인자로 사용하는 패턴.
 *   예: System.out.println(add(multiply(2, 3), 4));
 * =====================================================
 */

public class P02_MethodParameters {

    // =========================================================
    // 문제 1: 기본형 매개변수 - 값 전달 (Call by Value) 확인
    // =========================================================
    /*
     * 【문제 설명】
     * 정수 값을 매개변수로 받아 10을 더하는 메소드를 작성하세요.
     * 메소드 호출 전후로 원본 변수의 값이 변하지 않음을 확인하세요.
     *
     * 【예상 출력】
     *   메소드 호출 전 num: 5
     *   메소드 내부에서 변경 후: 15
     *   메소드 호출 후 num: 5   ← 원본은 변하지 않음!
     *
     * 【힌트】
     * - 메소드 안에서 매개변수를 변경하고 출력하세요.
     * - main에서 메소드 호출 전후의 변수 값을 출력하여 비교하세요.
     * - 기본형은 값이 복사되므로 원본이 변하지 않습니다.
     */
    static void addTen(int number) {
        // TODO: number에 10을 더하고 "메소드 내부에서 변경 후: [값]" 출력
    }

    // =========================================================
    // 문제 2: 배열 매개변수 - 참조 전달 (Call by Reference) 확인
    // =========================================================
    /*
     * 【문제 설명】
     * 정수 배열을 매개변수로 받아 모든 요소에 2를 곱하는 메소드를 작성하세요.
     * 메소드 호출 전후로 원본 배열의 값이 변경됨을 확인하세요.
     *
     * 【예상 출력】
     *   메소드 호출 전: [1, 2, 3, 4, 5]
     *   메소드 호출 후: [2, 4, 6, 8, 10]  ← 원본 배열이 변경됨!
     *
     * 【힌트】
     * - for문으로 배열의 각 요소에 2를 곱하세요.
     * - 배열은 참조형이므로 메소드 내에서 변경하면 원본도 변합니다.
     * - 배열 출력 헬퍼 메소드 printArray()를 아래에 작성했습니다.
     */
    static void doubleArray(int[] arr) {
        // TODO: 배열의 모든 요소에 2를 곱하는 코드 작성
    }

    // 배열 출력 헬퍼 메소드 (시그니처만 제공 - 내용 구현 필요)
    static void printArray(int[] arr) {
        // TODO: "[요소1, 요소2, ...]" 형식으로 배열 출력
        //       예시: [1, 2, 3, 4, 5]
    }

    // =========================================================
    // 문제 3: 배열을 반환하는 메소드
    // =========================================================
    /*
     * 【문제 설명】
     * 1부터 n까지의 정수를 담은 배열을 생성하여 반환하는 메소드를 작성하세요.
     *
     * 【예상 출력】
     * int[] arr = createRange(5) 호출 시:
     *   생성된 배열: [1, 2, 3, 4, 5]
     *
     * int[] arr = createRange(3) 호출 시:
     *   생성된 배열: [1, 2, 3]
     *
     * 【힌트】
     * - 반환형을 int[]로 선언하세요.
     * - new int[n]으로 크기 n의 배열을 생성하세요.
     * - for문으로 배열에 1부터 n까지 값을 채우세요.
     * - return으로 배열을 반환하세요.
     */
    static int[] createRange(int n) {
        // TODO: 크기 n의 배열을 생성하고 1~n까지 값을 채워서 반환
        return null; // 임시 반환값 (수정 필요)
    }

    // =========================================================
    // 문제 4: 메소드 체이닝 패턴 이해
    // =========================================================
    /*
     * 【문제 설명】
     * 아래 헬퍼 메소드들을 이용하여 메소드 체이닝 방식으로 계산하는
     * 예제를 main 메소드에 작성하세요.
     *
     * 수행할 계산:
     *   (1) 5와 3을 곱한 결과를 즉시 add()에 전달하여 10을 더한다.
     *   (2) 위 결과를 즉시 square()에 전달하여 제곱한다.
     *   (3) 최종 결과를 출력한다.
     *
     * 【예상 출력】
     *   단계별 계산:
     *   5 × 3 = 15
     *   15 + 10 = 25
     *   25² = 625
     *   체이닝 결과: 625
     *
     * 【힌트】
     * - 단계별: int step1 = multiply(5, 3); int step2 = addValue(step1, 10); ...
     * - 체이닝: int result = square(addValue(multiply(5, 3), 10));
     * - 두 방식을 비교해보세요.
     */
    static int multiply(int a, int b) {
        // TODO: a와 b의 곱을 반환
        return 0; // 임시 반환값 (수정 필요)
    }

    static int addValue(int a, int b) {
        // TODO: a와 b의 합을 반환
        return 0; // 임시 반환값 (수정 필요)
    }

    static int square(int n) {
        // TODO: n의 제곱을 반환
        return 0; // 임시 반환값 (수정 필요)
    }

    public static void main(String[] args) {

        System.out.println("=== 문제 1: Call by Value - 기본형 값 전달 확인 ===");
        // TODO: int num = 5; 선언 후 메소드 호출 전/후 num 값 출력
        //       addTen(num) 호출

        System.out.println("\n=== 문제 2: Call by Reference - 배열 참조 전달 확인 ===");
        // TODO: int[] numbers = {1, 2, 3, 4, 5}; 선언
        //       메소드 호출 전 배열 출력
        //       doubleArray(numbers) 호출
        //       메소드 호출 후 배열 출력

        System.out.println("\n=== 문제 3: 배열을 반환하는 메소드 ===");
        // TODO: createRange(5)를 호출하여 반환된 배열을 변수에 저장하고 출력
        //       createRange(3)도 동일하게 수행

        System.out.println("\n=== 문제 4: 메소드 체이닝 패턴 ===");
        // TODO: 단계별 방식으로 5 × 3, + 10, 제곱 계산 후 각 단계 출력
        //       이후 체이닝 방식으로 동일 계산을 한 줄로 작성
    }
}
