/*
 * =====================================================
 * 파일명: P03_MethodOverloading.java
 * 주제: 메소드 오버로딩 (Method Overloading)
 * =====================================================
 *
 * 【학습 목표】
 * 1. 메소드 오버로딩의 개념과 규칙을 이해한다.
 * 2. 동일한 이름으로 다양한 타입/개수의 매개변수를 처리하는 메소드를 작성한다.
 * 3. 오버로딩(Overloading)과 오버라이딩(Overriding)의 차이를 명확히 구분한다.
 *
 * 【핵심 개념】
 * - 메소드 오버로딩(Overloading):
 *   같은 클래스 내에서 동일한 이름의 메소드를 여러 개 정의하는 것.
 *   단, 매개변수의 타입, 개수, 순서가 달라야 합니다.
 *   반환형만 다른 경우는 오버로딩이 아닙니다.
 *
 * - 오버로딩 성립 조건:
 *   (1) 메소드 이름이 동일해야 함
 *   (2) 매개변수 목록(타입, 개수, 순서)이 달라야 함
 *   → 컴파일 시점에 어떤 메소드를 호출할지 결정됨 (정적 다형성)
 *
 * - 오버로딩 vs 오버라이딩:
 *   오버로딩(Overloading): 같은 클래스에서 이름 같고 매개변수 다른 메소드 여러 개 정의
 *   오버라이딩(Overriding): 부모 클래스의 메소드를 자식 클래스에서 재정의
 *   → 자세한 설명은 문제 3 주석 참조
 * =====================================================
 */

public class P03_MethodOverloading {

    // =========================================================
    // 문제 1: add() 메소드 오버로딩
    // =========================================================
    /*
     * 【문제 설명】
     * 아래 세 가지 버전의 add() 메소드를 오버로딩으로 구현하세요.
     *   (1) int 두 개를 더하는 add(int a, int b)
     *   (2) double 두 개를 더하는 add(double a, double b)
     *   (3) int 세 개를 더하는 add(int a, int b, int c)
     *
     * 【예상 출력】
     *   add(3, 5) = 8
     *   add(1.5, 2.3) = 3.8
     *   add(1, 2, 3) = 6
     *
     * 【힌트】
     * - 메소드 이름은 모두 add로 동일하게 작성하세요.
     * - 매개변수의 타입과 개수로 어떤 메소드가 호출될지 구분됩니다.
     * - double 결과는 소수점 한 자리까지 출력하려면 %.1f 형식 사용.
     */
    static int add(int a, int b) {
        // TODO: a + b 결과 반환
        return 0; // 임시 반환값 (수정 필요)
    }

    static double add(double a, double b) {
        // TODO: a + b 결과 반환
        return 0.0; // 임시 반환값 (수정 필요)
    }

    static int add(int a, int b, int c) {
        // TODO: a + b + c 결과 반환
        return 0; // 임시 반환값 (수정 필요)
    }

    // =========================================================
    // 문제 2: print() 다양한 타입 오버로딩
    // =========================================================
    /*
     * 【문제 설명】
     * 서로 다른 타입의 값을 출력하는 print() 메소드를 오버로딩으로 구현하세요.
     *   (1) print(int value)    → "정수: [값]" 출력
     *   (2) print(double value) → "실수: [값]" 출력
     *   (3) print(String value) → "문자열: [값]" 출력
     *   (4) print(boolean value)→ "논리값: [값]" 출력
     *
     * 【예상 출력】
     *   print(42) 호출 시:      정수: 42
     *   print(3.14) 호출 시:    실수: 3.14
     *   print("안녕") 호출 시:  문자열: 안녕
     *   print(true) 호출 시:    논리값: true
     *
     * 【힌트】
     * - 각 메소드는 매개변수 타입만 다르고 이름은 print로 동일합니다.
     * - Java는 호출 시 전달된 인자의 타입에 따라 적절한 메소드를 자동 선택합니다.
     */
    static void print(int value) {
        // TODO: "정수: [value]" 출력
    }

    static void print(double value) {
        // TODO: "실수: [value]" 출력
    }

    static void print(String value) {
        // TODO: "문자열: [value]" 출력
    }

    static void print(boolean value) {
        // TODO: "논리값: [value]" 출력
    }

    // =========================================================
    // 문제 3: 오버로딩(Overloading) vs 오버라이딩(Overriding) 비교
    // =========================================================
    /*
     * 【문제 설명】
     * 아래 주석을 읽고 두 개념의 차이를 정리한 후,
     * 오버로딩의 예시 메소드 2개를 추가로 작성하세요.
     *
     * ┌─────────────┬──────────────────────────┬──────────────────────────┐
     * │  구분        │ 오버로딩 (Overloading)    │ 오버라이딩 (Overriding)  │
     * ├─────────────┼──────────────────────────┼──────────────────────────┤
     * │ 정의         │ 같은 클래스에서           │ 부모 클래스의 메소드를   │
     * │             │ 같은 이름의 메소드를       │ 자식 클래스에서 재정의   │
     * │             │ 여러 개 선언              │                          │
     * ├─────────────┼──────────────────────────┼──────────────────────────┤
     * │ 클래스 관계  │ 동일한 클래스 내          │ 상속 관계 필요           │
     * ├─────────────┼──────────────────────────┼──────────────────────────┤
     * │ 메소드 이름  │ 동일                      │ 동일                     │
     * ├─────────────┼──────────────────────────┼──────────────────────────┤
     * │ 매개변수     │ 달라야 함                 │ 동일해야 함              │
     * ├─────────────┼──────────────────────────┼──────────────────────────┤
     * │ 반환형       │ 달라도 됨 (단독으로는 X)  │ 동일해야 함              │
     * ├─────────────┼──────────────────────────┼──────────────────────────┤
     * │ 다형성 종류  │ 정적 다형성 (컴파일 시)   │ 동적 다형성 (런타임 시)  │
     * ├─────────────┼──────────────────────────┼──────────────────────────┤
     * │ 어노테이션   │ 없음                      │ @Override 사용 권장      │
     * └─────────────┴──────────────────────────┴──────────────────────────┘
     *
     * 【문제】
     * describe(String s) 와 describe(int n) 을 오버로딩으로 작성하세요.
     * describe(String s) → "설명: [s]는 문자열입니다." 출력
     * describe(int n)    → "설명: [n]은 정수입니다." 출력
     *
     * 【힌트】
     * - 오버로딩은 @Override 어노테이션을 사용하지 않습니다.
     */
    static void describe(String s) {
        // TODO: "설명: [s]는 문자열입니다." 출력
    }

    static void describe(int n) {
        // TODO: "설명: [n]은 정수입니다." 출력
    }

    // =========================================================
    // 문제 4: 면적 계산기 오버로딩 (원, 사각형, 삼각형)
    // =========================================================
    /*
     * 【문제 설명】
     * 도형의 면적을 계산하는 area() 메소드를 오버로딩으로 구현하세요.
     *   (1) area(double radius)          → 원의 넓이: π × r²
     *   (2) area(double width, double height) → 사각형의 넓이: w × h
     *   (3) area(double base, double height, boolean isTriangle)
     *                                    → 삼각형의 넓이: (밑변 × 높이) / 2
     *       (isTriangle는 삼각형임을 구분하기 위한 플래그)
     *
     * 【예상 출력】
     *   원(반지름=5)의 넓이: 78.54
     *   사각형(가로=4, 세로=6)의 넓이: 24.00
     *   삼각형(밑변=3, 높이=8)의 넓이: 12.00
     *
     * 【힌트】
     * - 원주율: Math.PI 상수를 사용하세요.
     * - 결과는 double로 반환하세요.
     * - 출력 형식: %.2f 사용 (소수점 두 자리)
     */
    static double area(double radius) {
        // TODO: 원의 넓이 계산 후 반환 (Math.PI * radius * radius)
        return 0.0; // 임시 반환값 (수정 필요)
    }

    static double area(double width, double height) {
        // TODO: 사각형의 넓이 계산 후 반환 (width * height)
        return 0.0; // 임시 반환값 (수정 필요)
    }

    static double area(double base, double height, boolean isTriangle) {
        // TODO: 삼각형의 넓이 계산 후 반환 (base * height / 2)
        return 0.0; // 임시 반환값 (수정 필요)
    }

    public static void main(String[] args) {

        System.out.println("=== 문제 1: add() 메소드 오버로딩 ===");
        // TODO: add(3, 5), add(1.5, 2.3), add(1, 2, 3) 호출 및 결과 출력

        System.out.println("\n=== 문제 2: print() 다양한 타입 오버로딩 ===");
        // TODO: print(42), print(3.14), print("안녕"), print(true) 순서로 호출

        System.out.println("\n=== 문제 3: 오버로딩 vs 오버라이딩 - describe() ===");
        // TODO: describe("Java"), describe(2024) 호출 및 결과 출력

        System.out.println("\n=== 문제 4: 면적 계산기 오버로딩 ===");
        // TODO: area(5.0), area(4.0, 6.0), area(3.0, 8.0, true) 호출 후
        //       각 도형 이름과 넓이를 소수점 두 자리로 출력
    }
}
